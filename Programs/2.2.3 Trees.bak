(define my-list (list (list 1 2) (list 3 4)))

(define (count-leaves tree)
  (cond ((null? x) 0)
        ((not (pair? x)) 1)
        (else (+ (coutn-leaves (car x))
                 (count-leaves (cdr x))))))